name: Test

on:
  workflow_run:
    workflows: ["Build"]
    types: ["completed"]

jobs:
  meson-test:
    name: Run
    runs-on: ubuntu-latest
    steps:
      - name: Verbosely Test
        run: |
          meson test -v -C build
